import { Card, CardActions, CardContent, CardHeader } from '@mui/material';

export function QuestionCard() {
  return (
    <Card component='article'>
      <CardHeader>QUESTION CARD</CardHeader>
      <CardContent>QUESTION CARD CONTENT</CardContent>
      <CardActions>QUESTION CARD ACTIONS</CardActions>
    </Card>
  );
}
